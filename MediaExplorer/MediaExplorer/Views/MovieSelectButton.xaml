<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MediaExplorer.ViewModels"
             x:Class="MediaExplorer.Views.MovieSelectButton"
             x:DataType="viewmodels:MovieSelectButtonViewModel">
    <ContentView.Content>
        <Frame BackgroundColor="#202027" >
            <StackLayout>
                <!--<Image Source="{Binding ImageSrc}" Aspect="AspectFill" Scale="0.5"/>-->
                <Label Text="{Binding Name}" />
                <Label Text="{Binding Description}"/>
                <Button Command="{Binding PlayMovie}" Text="Play Movie"/>
                <Button Command="{Binding PlayTrailer}" Text="Play Trailer"/>
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>